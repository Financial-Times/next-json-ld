{
  "name": "@financial-times/next-json-ld",
  "version": "0.0.0",
  "description": "Helpers for producing schema.org markup in JSON LD on ft.com",
  "main": "main.js",
  "devDependencies": {
    "@financial-times/n-gage": "^1.6.2",
    "eslint": "^3.15.0",
    "lintspaces-cli": "^0.6.0",
    "mocha": "^3.1.2",
    "npm-prepublish": "^1.2.2"
  },
  "dependencies": {
    "html-to-text": "^2.1.3",
    "wordcount": "^1.1.1"
  },
	"config": {
    "pre-git": {
      "pre-commit": [
        "node_modules/.bin/secret-squirrel",
        "make verify -j3"
      ]
    }
  }
}
