{
  "name": "next-json-ld",
  "version": "1.0.0",
  "description": "Converts data from the next ElasticSearch into JSON-LD for schema.org",
  "main": "main.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "mocha tests/"
  },
  "devDependencies": {
    "mocha": "^3.1.2"
  },
  "dependencies": {
    "html-to-text": "^2.1.3",
    "wordcount": "^1.1.1"
  }
}
